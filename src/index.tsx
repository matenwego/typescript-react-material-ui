import * as React from "react";
import * as ReactDom from "react-dom";

import {ImplementedExample } from "./components";

ReactDom.render(
    <ImplementedExample />,
    document.getElementById("example")
);